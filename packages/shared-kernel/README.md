# TODO

Move the OutboxPattern out of the boilerplate.

- **Goal:** keep the boilerplate lightweight and build features in separate, focused modules.
- **Why:** It felt heavy for many use cases. I prefer an opt-out/in approach
